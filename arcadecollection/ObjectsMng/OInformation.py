#Массив всех созданных объектов
OArray = []

class OArrayTag:

    #В планах (:
    def __init__(self, tag, number):
        for obj in OArray:
            if obj.tag == tag:
                pass



class OInformation:

    #Тэг объекта
    tag = 'Object'